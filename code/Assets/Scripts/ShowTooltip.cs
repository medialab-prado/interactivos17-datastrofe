using UnityEngine;
using UnityEngine.UI;
using UnityEngine.Events;
using System.Collections;


public class ShowTooltip : MonoBehaviour {
	/*
	private TooltipPanel tooltipPanel;
	private static ShowTooltip instance;

	void Awake () {
		tooltipPanel = TooltipPanel.Instance ();
	}

	public static ShowTooltip Instance
	{
		get { return instance ?? (instance = new GameObject("ShowTooltip").AddComponent<ShowTooltip>()); }
	}

	public void Show(
		string title,
		string contaminacion,
		string ruido,
		string trafico,
		string coste,
		string income,
		Vector3 pos) {
		tooltipPanel.Show(title, contaminacion, ruido, trafico, coste, income, pos);
	}



	public  void Hide() {
		tooltipPanel.Hide();
	}
			
**/


}